name: Déployer sur GitHub Pages

on:
  push:
    branches: [claude/badminton-rankings-management-uiea65, main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run build
        env:
          GITHUB_PAGES: true
          # Clé "publishable" Supabase : conçue pour être exposée côté client
          # (protégée par les policies RLS), donc sans risque à committer ici.
          VITE_SUPABASE_URL: https://jwsyjgmtfcmxooqacydd.supabase.co
          VITE_SUPABASE_PUBLISHABLE_KEY: sb_publishable_0yOSTQTWAmzH71MnZKeynw_CC-Bbz13
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
